# 科研文献 AI 助手 —— 项目说明

> 本文档用于快速了解本项目：它是什么、解决什么问题、怎么工作的、怎么运行、有哪些文件、有什么局限。

---

## 一、项目简介

**科研文献 AI 助手**是一个基于 **RAG（检索增强生成，Retrieval-Augmented Generation）** 的最小可用产品（Demo）：

- 用户上传论文 PDF；
- 系统自动解析、建立索引；
- 用户用自然语言提问（如"这篇论文的核心结论是什么？"）；
- 系统先检索论文中最相关的段落，再让大模型**照着这些段落回答**，并标注参考来源。

**核心价值**：让 AI 的回答"有据可查、不瞎编"，适合对准确性和可溯源要求高的科研场景。

---

## 二、解决的问题

| 痛点 | 本项目的解法 |
|---|---|
| 几十页英文论文难快速读懂 | 上传后直接对全文提问，快速定位创新点、方法、结论 |
| 直接问 ChatGPT 可能"幻觉"编造内容 | RAG 把答案锚定在论文原文上，检索不到就明说"文献中没有相关内容" |
| 学术场景要求可溯源 | 回答附「参考来源」（命中的文献名） |

**为什么用 RAG 而不是微调（fine-tuning）**：

| 对比项 | RAG | 微调 |
|---|---|---|
| 本质 | 不改模型，给它"开卷考试" | 改模型参数，让它"背下来" |
| 知识更新 | 换 PDF 即生效，零训练 | 需重新训练，慢且贵 |
| 成本 | 低（按 API 调用付费） | 高（需 GPU 训练） |
| 可解释性 | 可指出答案来自哪一段 | 黑盒，说不出依据 |

科研文献"更新快、要溯源、预算有限"，三点全部命中 RAG。

---

## 三、整体架构与数据流

```
① 离线阶段（上传 PDF 时执行一次）
   论文PDF ──解析(pypdf)──> 全文文本 ──切块(chunk_size=500, overlap=80)──> 若干小段
        ──向量化(BAAI/bge-m3)──> 向量索引（内存列表）

② 在线阶段（每次提问）
   用户问题 ──向量化──> 问题向量
        │
        ▼
   余弦相似度比对 ──> 检索最相关的 top-3 个小段
        │
        ▼
   把「检索到的段落 + 用户问题」拼成提示词
        │
        ▼
   大模型(DeepSeek-V3) 基于段落生成答案，标注来源
```

**一句话总结**：论文先被"切碎、转成向量"存起来；提问时把问题也转成向量，用"向量算距离"找出最像的几段，塞给大模型当参考资料，让它"照本宣科"。

---

## 四、技术栈与文件结构

**技术栈**：Python 3.9+、Streamlit（界面）、pypdf（PDF 解析）、numpy（向量计算）、requests（调用 API）。
外部服务使用**硅基流动 SiliconFlow**（OpenAI 兼容接口），一个 API Key 同时支持向量化与对话。

| 文件 | 作用 |
|---|---|
| `config.py` | 配置：API Key、接口地址、向量化/对话模型名 |
| `rag.py` | 核心逻辑：解析 → 切块 → 向量化 → 检索 → 生成（含 `DocIndex` 索引类） |
| `app.py` | Streamlit 界面：侧边栏上传 PDF，主区对话 |
| `test_api.py` | 连通性自检：验证 Key 与两个模型是否可用 |
| `requirements.txt` | 依赖清单：streamlit / pypdf / numpy / requests |
| `README.md` | 环境准备、运行教程、常见问题 |

---

## 五、如何运行

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 在 config.py 填入自己的 SiliconFlow API Key（https://siliconflow.cn）

# 3. 自检 Key 与模型
python test_api.py

# 4. 启动界面
streamlit run app.py
# 浏览器打开 http://localhost:8501
```

---

## 六、关键实现细节

- **切块**：每块 500 字符、相邻块重叠 80 字符，避免句子被从中间切断；过滤过短块。
- **向量化**：调用 `/embeddings` 接口，返回 1024 维向量（`BAAI/bge-m3`）。
- **检索**：余弦相似度（cosine similarity）排序，取 top-3。
- **生成**：`temperature=0.3` 降低随机性；system 提示词强制"只根据文献回答，没有就直说，绝不编造"。
- **状态保持**：索引存于 Streamlit `session_state`，刷新页面不丢失；同一文件不会重复载入。

---

## 七、当前局限与可改进方向

1. **无持久化存储**：向量索引只存在内存，重启即丢失 → 可引入向量数据库（如 Chroma、FAISS、Milvus）。
2. **不支持扫描版 PDF**：图片型 PDF 提取不到文字 → 可接入 OCR（如 PaddleOCR）。
3. **切块策略简单**：按字符硬切，可能切断章节 → 可改用按段落/语义切块。
4. **检索精度有限**：仅用余弦相似度 → 可加 BM25 混合检索（hybrid search）、重排序（rerank）。
5. **无效果评测**：没有标注数据集 → 可构建 QA 评测集衡量召回率与回答准确率。
6. **安全性**：`config.py` 中明文存放了 API Key，若需公开代码应改为环境变量。

---

## 八、适用场景

- **学习/教学**：快速搞懂一篇论文，验证 RAG 全流程；
- **作品集**：展示工程实现能力；
- **个人知识库雏形**：可扩展为"上传任意文档、对文档提问"的通用助手。
